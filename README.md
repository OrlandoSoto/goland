- Install go: https://go.dev/doc/install

- Clone this repo

- On your terminal type `go run ./main.go`

Output should look like this:

```
Mallocs:  165
HeapAlloc:  182936
numCPU:  8
```

This program invokes the `runtime` library and calls two functions to query for CPU and RAM resources on the host system

